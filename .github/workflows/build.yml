name: Build linux-amd64

on:
  push:
    branches: [main, feature/mongodb-support]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.24'

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc

      - name: Build
        env:
          CGO_ENABLED: "1"
          GOOS: linux
          GOARCH: amd64
        run: |
          go build -o x-ui-linux-amd64 -ldflags="-s -w" main.go

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: x-ui-linux-amd64
          path: x-ui-linux-amd64
